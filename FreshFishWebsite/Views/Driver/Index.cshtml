@model IEnumerable<OrderItems>
@{
    Layout = "~/Views/Shared/_Driver_Layout.cshtml";
    ViewData["Title"] = "Доставки";
}
@* <table class="table">
    <tr>
        <td>Дата замовлення</td>
        <td>Адреса отримувача</td>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Order.OrderDate</td>
            <td>@item.Order.User.CompanyAddress</td>
            <td>
                <a class="btn btn-sm btn-primary" asp-action="GetOrderDetails" asp-route-id="@item.Id">Деталі</a>
            </td>
        </tr>
    }
</table> *@
<div class="container-xxl">
    <h6 class="text-secondary">Список доставок</h6>
<div class="table-responsive-sm position-relative">
        <table  class="table table-striped table-bordered caption-top basic-table">
            <thead>
                <tr>
                    <th scope="col col-sm-1" class="h5">#</th>
                    <th scope="col" class="h5">Дата замовлення</th>
                    <th scope="col" class="h5 col-sm-4">Адреса отримувача</th>
                    <th scope="col" class="h5 col-sm-1">Дія</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
    {
        <tr>
             <td>@item.Order.Id</td>
            <td>@item.Order.OrderDate</td>
            <td>@item.Order.User.CompanyAddress</td>
            <td>
                <a class="btn btn-sm btn-primary" asp-action="GetOrderDetails" asp-route-id="@item.Id">Деталі</a>
            </td>
        </tr>
    }
            </tbody>
        </table>
    </div>
    </div>